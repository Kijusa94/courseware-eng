# Dependencias de ejecución para renderizar los chunks Python de Quarto.
# Python >= 3.10 recomendado. Instalar con: pip install -r requirements.txt

# Kernel Jupyter requerido por Quarto para ejecutar chunks {python}
ipykernel>=6.29

# Librerías usadas por los scripts de prácticas y presentaciones
numpy>=1.26
pandas>=2.1
matplotlib>=3.8
schemdraw>=0.19
xlsxwriter>=3.1

# CLI de Quarto (instalador oficial vía pip)
quarto-cli>=1.8
