# Cargar renv (reproducibilidad del entorno R).
# En la primera apertura del proyecto, renv instalará la versión
# indicada en renv.lock dentro de la librería privada del proyecto.
source("renv/activate.R")
